[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd petcare_advisor && PYTHONPATH=src python -m uvicorn petcare_advisor.main:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
