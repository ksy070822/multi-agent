[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd petcare_advisor && PYTHONPATH=src uvicorn petcare_advisor.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

